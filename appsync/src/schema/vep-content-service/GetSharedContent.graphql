extend type Query {
    GetSharedContent(
        fair: String,
        slug: String,
        lang: String
    ): SharedContent

    GetPreviewSharedContent(
        fair: String,
        slug: String,
        lang: String
    ): SharedContent
}

type SharedContent {
  timestamp: String
  status: String
  data: String
}

