extend type Query {
    GetPressReleaseJson(
        fair: String,
        lang: String,
        slug: String
    ): PressReleaseJson

    GetPreviewPressReleaseJson(
        fair: String,
        lang: String,
        slug: String
    ): PressReleaseJson
}

type PressReleaseJson {
  timestamp: String
  status: String
  data: String
}

